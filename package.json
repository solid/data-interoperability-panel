{
  "name": "data-interoperability-panel",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "node scripts/prepare-export.js"
  },
  "main": "dist/index.json",
  "license": "MIT",
  "dependencies": {
    "@mermaid-js/mermaid-cli": "^8.10.1"
  }
}
